// Scroll Plugin
import SwupScrollPlugin from '@swup/scroll-plugin';

// Install the plugin
const scrollPlugin = new SwupScrollPlugin();

// Add the plugin to the swup instance
const swup = new Swup({
  plugins: [scrollPlugin]
});
